# AcioleUI design

Use AcioleUI como sistema visual principal. Reutilize componentes, tokens semânticos, `ThemeProvider`, `useTheme` e ícones antes de criar equivalentes locais.

Mantenha componentes em pastas próprias, separe regra de negócio da UI, preserve acessibilidade e valide TypeScript/build.
