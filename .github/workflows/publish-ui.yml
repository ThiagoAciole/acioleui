name: Release acioleui

on:
  push:
    branches: [main]
    paths:
      - 'packages/ui/src/**'
      - 'packages/ui/package.json'
      - 'packages/ui/vite.config.ts'
      - 'package-lock.json'
  workflow_dispatch:

concurrency:
  group: publish-acioleui
  cancel-in-progress: false

jobs:
  publish:
    if: github.event_name == 'workflow_dispatch' || !contains(github.event.head_commit.message, '[skip ci]')
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: npm ci

      - name: Check vulnerabilities
        run: npm audit --audit-level=high

      - name: Bump patch version
        run: npm version patch --workspace=packages/ui --no-git-tag-version

      - name: Build package
        run: npm run build --workspace=packages/ui

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Commit and push version bump
        run: |
          git add packages/ui/package.json package-lock.json
          git commit -m "chore: release acioleui [skip ci]"
          git push

      - name: Publish to npm
        run: npm publish --workspace=packages/ui --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
